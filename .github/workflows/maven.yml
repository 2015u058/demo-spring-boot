version: 0.2

phases:
  install:
    runtime-versions:
      java: 17
    commands:
      - echo "Installing Maven..."
      - apt-get -y update
      - apt-get -y install maven
  build:
    commands:
      - echo "Building with Maven..."
      - mvn clean verify -DskipTests
